rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    match /tasks/{taskId} {
      allow read: if true;
      allow write: if false; // dashboard is read-only; writes stay server-side
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
